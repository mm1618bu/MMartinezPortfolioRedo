doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title LeetcodeClone - Problem
    link(rel="stylesheet" href="/src/styles.scss")
  body
    header.top-nav
      .container
        .page
          .card.card--compact
            .card__header
              h1.card__title Two Sum
              .tabs
                button.tab.is-active(type="button") Problem
                button.tab(type="button") Submissions

    main.container.page
      .split-pane
        aside.split-pane__left.card
          .card__header
            h2.card__title Problem
            span.badge.badge--easy Easy
          .card__body
            p Given an array of integers, return indices of the two numbers such that they add up to target.
            h3 Constraints
            ul
              li 2 <= nums.length <= 10^4
              li -10^9 <= nums[i] <= 10^9
              li -10^9 <= target <= 10^9

            h3 Example
            pre.monospace
              | Input: nums = [2,7,11,15], target = 9
              | Output: [0,1]

        .split-pane__resizer(aria-hidden="true")
        section.split-pane__right.card
          .card__header
            h2.card__title Editor
            .tabs
              button.tab.is-active(type="button") JavaScript
              button.tab(type="button") Python
          .editor-shell
            #editor
          .card__header
            h3.card__title Console
            .tabs
              button.tab.is-active(type="button") Run
              button.tab(type="button") Submit
          pre.monospace
            | Ready.

    script(type="module" src="/src/main.js")